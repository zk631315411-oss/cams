# .understandignore - patterns for files and directories excluded from analysis
# Syntax matches .gitignore. Built-in exclusions also cover common dependencies,
# build output, binary assets, and lockfiles.

# Suggested project-specific exclusions (uncomment if these areas are out of scope).
# tests/

# Test file patterns (uncomment to exclude).
# *.test.*
# *.spec.*
# *.snap
