# CAMS V7 新题加入技术执行与现状限制

> **文档定位：** 本文件保存 2026-07-29 时点旧研发仓库的新题处理技术实现、文件关系、脚本限制、临时绕行方案和执行风险，供追溯。正式工作台已经迁至 `D:/守正公司工作区/cams考试工作台（正式版）/`，新题处理以该目录 README 和工作流实现为准。

> **使用边界：** 文中路径、命令、字段和程序行为会随正式工作台建设而变化。任何执行动作都应先确认当前源码、输入输出位置和发布门禁；明确标为停用或存在覆盖风险的步骤不得直接用于生产目录。

> 版本：v1.2
>
> 适用范围：真题回忆、第三方题库题目，以及已有题目的实质性修订。
>
> 核心原则：DS 用于初检索和草稿生成；Codex 负责独立核验与补证；人工审核后才能发布。正式 CAMS 解析必须以 v7 教材证据为基础。

> **使用前提：** 本 SOP 只处理“现有题库下增加新题”。它默认当前教材版本、教材证据段、知识地图、检索能力、解析模板和发布规则已经存在且可用；不重复进行 PDF 教材转换、全书知识分割、知识图谱重建或全题库解析重跑。若引入新教材或整体新建题库，应改用《新教材、新题库解析撰写 SOP》。

> **2026-07-28 现状停用提示：** 经核验，当前 `CAMS_v7_questions.jsonl` 已是清洗后的运行时结构，而 `prepare_questions.py` 仍读取旧字段。故本文件中“新增题写入 JSONL 后运行 `prepare_questions.py`”的操作和命令**当前不得执行**，否则会生成空题并覆盖 Phase 3.5 输出。新增题入口已由正式工作台承接；其余内容只按历史研发限制理解。

> 当前实现提醒：`prepare_questions.py` 会重写 Phase 3.5 的全量输出，且当前只写出中文 `stem/options` 等基础字段，不会把 JSONL 的 `stem_en/options_en` 自动带入运行时题库；`build_release.py` 当前只校验机器产物是否 `pipeline_status=ok`，尚不读取 Codex/人工批准状态。因此新增题必须在隔离修订中处理，并用“已批准题目暂存目录”控制发布输入，不能把脚本的技术通过误认为审核通过。

## 1. 总流程

```text
接收原题
  -> 结构化清洗与编号
  -> DS 初检索/初判，生成候选证据包
  -> Codex 独立核验、补充教材/外部证据
  -> 标记题目状态并形成可审核解析
  -> 人工审核
  -> 冻结发布、线上核验与留档
```

任何一步发现题目来源、题面、答案或证据存在实质问题，都应进入异常处理，不得通过补写解析绕过问题。

## 2. 步骤一：结构化清洗与编号

### 输入

- 原始题目文件、截图或题源记录。
- 已知的题干、选项、参考答案、语言版本和来源信息。

### 编号、来源归档与准入规则

1. 在 `教材、答疑记录、习题与参考文献/习题/v7习题/新增题源/YYYY-MM/` 保存原始文件、截图或网页导出；文件名使用 `YYYYMMDD_来源简称_外部题号或主题_原始扩展名`。此目录是新增题源的约定归档位置，需要在首次新增题前建立。
2. 同目录维护 `来源台账.jsonl`。每题至少记录：`question_id`、JSONL `id`、`external_source_id`（第三方原始题号或来源内临时号）、原件相对路径、来源类型（真题回忆/第三方题库）、来源可信度（高/中/低）、接收日期、接收人、版权/使用状态、原文是否完整。
3. 当前 `prepare_questions.py` **不会读取手填的 `question_id`**，而是根据 JSONL 中 `id` 字段末尾的数字生成，例如 `CAMS-V7-0396` 自动生成 `v7_q_000396`，并把该 `id` 写入运行时字段 `source_question_id`。因此新增题的 JSONL `id` 必须使用未占用的连续序号；先扫描既有 `id` 和运行时 `question_id`，取最大编号加 1，且编号永不复用。
4. 第三方原始题号不能被内部编号覆盖。由于现有转换脚本不会把额外来源字段带入运行时题库，原始题号、题源链接和版权信息必须保留在来源台账中；如在 JSONL 增加 `external_source_id`，也只作为源文件侧辅助字段，不能代替台账。若题源没有稳定题号，由接收人生成来源内临时号并在台账说明。
5. 真题回忆若题干、选项或答案不完整，可以登记，但须标为“低可信度/待补全”，停留在 `received` 或 `needs_source_clarification`，不得进入 DS 盲判。

### 操作

1. 保存原题，不覆盖、不美化原始内容。
2. 分配未占用的 JSONL `id`，并按脚本规则预登记其对应永久 `question_id`；题号、章节或展示顺序后续变化时不修改该 ID。
3. 按原题顺序写入 `options_cn/options_en` 的 `key`（A-D 或 A-F）。当前脚本以该字母作为选项标识，尚无独立 `option_id`；如调整选项顺序，必须留存题目修订记录并重新核验答案。
4. 结构化记录中文/英文题干、选项、题型、参考答案、来源、接收日期和原文件位置。
5. 检查选项是否缺失、重复，题型与答案数量是否一致，中英文是否明显错配。

### 输出

- 结构化题目记录。
- 原始题目来源记录。
- 格式、翻译、答案或重复题风险标记。

### 停止条件

题面不完整、来源无法追溯、答案数量与题型冲突、疑似重复但无法判定时，不进入 DS 检索，先由教研人工处理。

### JSONL 最小录入模板

以下是与当前 `prepare_questions.py` 兼容的最小样例。字段名应保持一致；空缺信息必须显式标记，不要伪造完整性。

```json
{
  "id": "CAMS-V7-0396",
  "topic_cn": "客户尽职调查",
  "topic_en": "Customer Due Diligence",
  "type": "single",
  "stem_cn": "中文题干",
  "stem_en": "English stem",
  "options_cn": [
    {"key": "A", "text": "中文选项 A"},
    {"key": "B", "text": "中文选项 B"},
    {"key": "C", "text": "中文选项 C"},
    {"key": "D", "text": "中文选项 D"}
  ],
  "options_en": [
    {"key": "A", "text": "English option A"},
    {"key": "B", "text": "English option B"},
    {"key": "C", "text": "English option C"},
    {"key": "D", "text": "English option D"}
  ],
  "answer_final": ["A"],
  "answer_status": "一致",
  "parse_status": {"cn": "ok", "en": "ok"},
  "raw_question_cn": "原始中文题面或空字符串",
  "raw_question_en": "原始英文题面或空字符串",
  "risk_flags": []
}
```

- 单选题 `type` 为 `single`，`answer_final` 仅含一个字母；多选题 `type` 为 `multiple`，答案字母按展示顺序写入数组。
- `topic_cn/topic_en` 用于章节初步映射；无法判断时留空并在 `risk_flags` 中标记 `section_alignment_uncertain`，不要猜测章节。
- 同一题的中英文选项标签与数量必须一致。暂时没有英文时，将 `parse_status.en` 标为非 `ok` 并转人工处理。
- `prepare_questions.py` 当前只读取中文题干和选项生成运行时题库，不会保留 `stem_en/options_en`。因此英文已录入不等于双语运行时数据已完成：必须走现有英文补丁的受控合并流程，或先改造转换脚本；在此之前该题不得按“双语已验收”发布。
- 此模板描述的是当前脚本可读取的最小字段，并不替代来源台账；来源、版权和可信度信息仍须保留在台账中。

## 3. 步骤二：DS 初检索与初判

### 目标

快速生成“候选证据包”，为后续核验节省检索时间。DS 的结论是草稿，不是可发布结论。

### 输入

- 已结构化的题干、全部选项、题型和中英文表达。
- 当前冻结的 v7 教材知识单元与检索索引。

### 运行前检查

运行人应先确认：

- 当前题目已出现在 `phase3.5_questions/output/v7_questions.json`，且未进入 rejected 清单。
- v7 冻结 unit、索引文件和可选 KG 文件存在且与当前教材版本一致。
- Python 环境具有项目依赖；DS 调用环境变量至少设置了 `DEEPSEEK_API_KEY`、`DS_API_KEY` 或 `DS_KEY` 之一。地址如非默认值，设置 `DEEPSEEK_BASE_URL` 或 `DS_BASE_URL`。
- 先用单题、单并发运行；成功标准不是“终端没有报错”，而是得到该题的 `q_v7_q_*.json`，其中候选池、选项判断、`pipeline_status` 和 `validation_issues` 均可读取。

若脚本无法启动、索引缺失、API 失败或输出格式异常，记录为 `pipeline_failed`，先修复运行环境；不得将此类失败误判为“教材没有证据”。

### 操作

1. 用“题干 + 各选项”分别检索，不只检索题干。
2. 召回可能相关的 v7 教材片段，保留 `unit_id`、章节、书内页/PDF 页和原文。
3. 对每个选项作初步判断，并说明其引用依据和不确定点。
4. 产出初步答案、初步解析及候选教材证据。

### 输出：DS 候选证据包

至少包含：

- `question_id`。
- 每个选项的初步判断。
- 候选 `unit_id`、教材原文、章节和页码。
- 初步答案及其理由。
- 未找到依据、引文薄弱、翻译不确定或多个选项均可能成立等风险说明。

### 边界

- DS 不得因为题库已有参考答案而反向拼接理由。
- DS 引用的片段只表示“候选相关”，尚未证明其能支持当前结论。
- DS 草稿不能直接生成学生端解析或进入发布包。

## 4. 步骤三：Codex 独立核验与补证

### 目标

把 DS 的候选草稿从“可能相关”提升为“可审计、可审核的证据链”。Codex 不是润色 DS 文案，而是重新验证题目与证据之间是否真正成立。

### Codex 必做检查

1. **核对引文真实性**：DS 所引 `unit_id`、原文、页码是否真实存在且彼此对应。
2. **核对论证相关性**：引文是否真的支持或反驳该选项，而非只出现相同关键词。
3. **核对题干限定**：教材证据的主体、行为、条件、时间/流程阶段、场景和语气是否与题干及选项一致。
4. **补充教材内证据**：DS 证据不足、错位或不完整时，重新检索 v7 教材，补足直接或必要的组合证据。
5. **识别异常题**：判断是否存在教材证据不足、题库参考答案冲突、题干歧义、翻译错误或超纲情况。
6. **外部资料核对（必要时）**：对于法规时效、外部事实、题源正确性或教材未覆盖问题，可查外部权威资料，但必须与教材证据分开记录。

### 外部资料的使用规则

- v7 教材内证据是正式 CAMS 解析的依据。
- 外部资料只能用于判断题目是否有效、是否需要修改、是否超纲，或作为明确标注的补充资料。
- 只能依赖外部资料成立的题，不得包装成“教材结论”。学生端应标明“教材未直接覆盖”或暂不发布。
- 外部资料需记录来源、发布日期/生效时间、适用地区或机构，以及引用原因。

### 输出：Codex 核验记录

至少包含：

- 已确认的教材内证据与页码。
- 对每个选项的最终判断及理由。
- 对 DS 草稿的纠正或补证说明。
- 是否使用外部资料，以及其与教材证据的边界。
- 题目最终状态和下一步动作。

### Codex 核验记录与写入权

当前没有自动化脚本写入该记录，统一采用以下约定：

- 文件位置：`cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/output/codex_review/<question_id>.json`。
- 写入人：被指定的 Codex 核验执行者；内容处理人不得自行把未核验题标为 `reviewable`。
- 最小字段：`question_id`、`reviewed_at`、`reviewer`、`ds_result_path`、`textbook_evidence`、`external_sources`、`codex_status`、`decision_summary`、`open_issues`。
- `codex_status` 只能为 `reviewable`、`needs_human_review`、`needs_evidence`、`external_only`、`question_conflict` 或 `rejected`。

最小结构示例：

```json
{
  "question_id": "v7_q_000396",
  "reviewed_at": "2026-07-28T10:00:00+08:00",
  "reviewer": "codex/<执行者标识>",
  "ds_result_path": "../questions/q_v7_q_000396.json",
  "textbook_evidence": [
    {"unit_id": "v7u_N000001", "role": "direct", "reason": "支持正确项的主体、条件和结论", "printed_page": "14", "pdf_page": 19}
  ],
  "external_sources": [],
  "codex_status": "reviewable",
  "decision_summary": "教材证据足以支持答案；DS 的第二条引用已删除。",
  "open_issues": []
}
```

## 5. 步骤四：题目状态与处理分流

| 状态 | 判定标准 | 后续动作 |
|---|---|---|
| 可审核 | Codex 已确认教材证据完整，题目、答案和解析逻辑成立 | 生成可审核解析，交人工审核 |
| 待人工审核 | 教材依据基本充分，但存在措辞、翻译、题源或教学表达争议 | 标明争议点，交教研裁决 |
| 待补证 | 未能找到足以支撑答案的教材内证据 | 暂不写成确定解析；继续检索或人工查书 |
| 超纲/外部依据 | 题目可能成立，但 v7 教材未直接覆盖，或必须依赖外部资料 | 标记范围，决定改题、补充标注或不纳入正式题库 |
| 题目/答案冲突 | 题干、参考答案或选项与教材证据明显冲突 | 保留证据，提交教研裁决；不发布 |
| 退回/弃用 | 来源、题面、版权、重复或逻辑问题无法解决 | 保留登记记录，不进入题库 |

### 流程状态机与责任边界

为避免“待补证”“已审核”等内容结论和流程进度混用，每题按下列状态流转。状态、操作者和时间必须同时记入来源台账或题目审核记录。

```text
received
  -> structured
  -> ds_done
  -> codex_reviewed
  -> reviewable
  -> approved
  -> released

任一步可转入：needs_source_clarification / needs_evidence /
needs_human_review / external_only / question_conflict / rejected / rework。
released 发现问题后 -> retracted -> rework 或 rejected。
```

| 状态转换 | 有权操作的人 | 必须留下的记录 |
|---|---|---|
| `received -> structured` | 内容处理人 | 来源台账、内部编号、JSONL 记录 |
| `structured -> ds_done` | 运行人 | DS 输出路径、运行时间、模型/索引版本、失败日志 |
| `ds_done -> codex_reviewed -> reviewable` 或异常状态 | Codex 核验执行者 | Codex 核验 JSON 和证据结论 |
| `reviewable -> approved/rework/hold/rejected` | 独立人工审核人 | 审核意见、审核人、审核时间、结论 |
| `approved -> released` | 发布操作人 | release ID、发布输入清单、线上验收记录 |
| `released -> retracted` | 发布操作人或负责人 | 撤回原因、影响范围、新修订链接 |

建议默认时限：来源/格式问题 1 个工作日内补齐；`needs_evidence`、`question_conflict` 和 `external_only` 在 2 个工作日内由教研负责人裁决去向。超期仍无结论的题转 `hold`，不得以“暂时可用”进入发布。

## 6. 步骤五：形成可审核解析

仅“可审核”或“待人工审核”的题目可进入本步骤。

解析必须基于 Codex 已确认的证据链，包含：

1. **考点**：说明考生需要识别或区分的能力。
2. **核心解析**：按“教材规则/定义 -> 题干信号 -> 正确项为何最符合”说明。
3. **错误项分析**：逐项说明错在概念、主体、条件、场景、流程阶段、范围/程度或证据不足的哪一处。
4. **易错提醒**：提炼下次可复用的判断规则，不重复大段解析。
5. **教材依据**：列出教材原文、章节和页码；外部资料如有，必须另列为补充资料。

禁止：编造教材原句、编造页码、把外部知识伪装成教材结论、因为答案已知而反向合理化。

## 7. 步骤六：人工审核与发布

### 人工审核人必须确认

- 题干、选项、答案和题型无误。
- Codex 引用的教材原文和页码准确。
- 正确项得到充分支持，错误项的错因成立。
- 中文/英文表达不存在影响答案的偏差。
- 外部资料没有被混入教材依据。
- 解析对考生有解释价值，且没有把不确定内容写成确定结论。

审核结论只能是：批准、退回修改、暂缓补证、题源/答案问题、弃用。编辑人与审核人应分离。

### 异常题裁决规则

- **教材证据不足**：可追加一次定向教材检索和一次 Codex 复核；仍不足则转 `hold` 或 `rejected`，不发布。
- **参考答案冲突**：由教研负责人基于题干、教材原页和题源记录裁决；未裁决前状态为 `question_conflict`。
- **外部依据题**：先判断是否应改写为教材覆盖范围内的题。允许保留时，须在学生端明确标注“教材未直接覆盖”，且不得纳入教材型考点统计。
- **题干/翻译歧义**：优先修订题面，保留原题与改动说明；无法修订则弃用。
- **疑似重复题**：按“题干语义 + 正确答案 + 关键证据”判断；完全重复合并为同一题记录，只有选项顺序变化时不新建题。考查同一考点但情境或判断条件不同的题可并存。

### 发布与留档

1. 仅发布已批准的题目版本。
2. 保存题目版本、Codex 核验记录、审核意见、教材证据、外部资料记录和发布批次 ID。
3. 导出成功不等于发布完成；进入第三方后台或学生端后，应逐题核对题干、选项、答案、解析和页码。
4. 发现线上错误时，立即标记撤回/返工，创建新修订，不覆盖历史记录。

### 当前发布器的操作限制

当前 `build_release.py` 不读取 `codex_status` 或人工审核状态，只要某题机器 `pipeline_status` 为 `ok` 就会把它标为已发布。因此正式发布前必须：

1. 从上一已验收发布批次的批准证据复制全部既有 `q_*.json`，再叠加**本批人工批准题目**的 `q_*.json`，形成单独的发布暂存目录，例如 `phase4_evidence/release_input/<release_id>/questions/`。不得只放本批新增题，否则发布器会把未放入证据目录的旧题标记为未发布。
2. 在暂存目录旁保存 `approved_questions.json`，列出 `question_id`、Codex 核验文件、人工审核人、批准时间和批准版本。
3. `build_release.py --evidence-dir` 只能指向这个暂存目录，不能直接指向包含全量机器结果的 `output/`。
4. 发布后核对 manifest 的 `published_questions` 数量、批准清单和线上展示题目数完全一致。

在发布器接入 `codex_status=reviewable` 与 `human_status=approved` 双门禁前，上述人工暂存控制是不可省略的临时措施。

## 8. 最小交付物清单

每道进入正式流程的新题，应至少留下以下六类记录：

1. 原始题目来源记录。
2. 结构化题目记录及永久 ID。
3. DS 候选证据包。
4. Codex 核验与补证记录。
5. 人工审核结论与意见。
6. 发布/撤回状态及对应版本。

缺少其中任何一项，题目都不应被视为可追溯的正式题库内容。

## 9. 当前文件、脚本与执行顺序

本节只列“新增一题”会直接读取、写入或依赖的当前 v7 文件。历史 v6 资产、早期试验脚本和未迁移的服务不属于本 SOP 的正式生产入口。

### 9.1 一题新增时的实际顺序

```text
原题来源记录
  -> CAMS_v7_questions.jsonl
  -> prepare_questions.py
  -> v7_questions.json + 质量报告 + 章节映射
  -> blind_adjudication.py --question-id <新题ID>
  -> q_<新题ID>.json（DS 候选证据包）
  -> Codex 独立核验与补证（当前为人工/Agent 关口）
  -> generate_evidence_explanations.py（生成机器解析草稿）
  -> review_check.py / quality_review.py
  -> build_review_source.py
  -> 人工审核
  -> build_release.py
```

其中，`build_index.py`、`build_textbook_release.py` 和知识图谱构建脚本不是每道新题都运行：只有教材、知识单元、术语或索引底座发生变化时才需要重跑。

### 9.2 文件与脚本清单

| 顺序 | 文件/脚本 | 作用 | 新题处理时如何使用 |
|---|---|---|---|
| 0 | 新收到的真题回忆、第三方题库文件、截图或链接 | 原始题源证据 | 保存原件并登记来源；不直接覆盖或美化原始材料 |
| 1 | `教材、答疑记录、习题与参考文献/习题/v7习题/v7结构化文本/CAMS_v7_questions.jsonl` | 结构化题库源文件，每行一题 | 将清洗后的新题按既有 schema 加入；这是 `prepare_questions.py` 的输入 |
| 1 | 同目录 `CAMS_v7_questions.schema.json` | `prepare_questions.py` 生成的运行时题库结构约束 | 用于核对输出的 `v7_questions.json`；JSONL 原始输入字段以 `prepare_questions.py` 的读取逻辑和本 SOP 的最小模板为准 |
| 1 | 同目录 `CAMS_v7_questions.md` / `.csv` / `.json` | 人读、表格导入和同步资产 | 由正式清洗结果同步生成或核对；运行时正式读取 Phase 3.5 输出 |
| 2 | `cams工作台（重构版）/v7/选项证据与解析生成/phase3.5_questions/prepare_questions.py` | 将 JSONL 标准化为 v7 运行时题库；统一 ID、题型、章节映射并产出质量报告 | 新题写入 JSONL 后运行；若报告显示解析/答案/章节问题，先处理异常，不进入盲判 |
| 2 | `phase3.5_questions/output/v7_questions.json` | 正式 v7 结构化题库，盲判和解析的统一题目输入 | 该文件由 `prepare_questions.py` 生成；不建议直接手改 |
| 2 | `phase3.5_questions/output/v7_question_section_map.json` | 题目到章节/小节的导航映射 | 用于按章审核、检索辅助和导出定位；不代替教材证据 |
| 2 | `phase3.5_questions/output/v7_question_quality_report.md` | 题库清洗质量报告 | 每次加题后阅读，处理格式、答案冲突和章节异常 |
| 2 | `phase3.5_questions/output/rejected_or_needs_review_questions.json` | 无法自动通过清洗的题目清单 | 出现在此文件的题先回到人工处理，不进入后续自动链路 |
| 基础依赖 | `v7/work/base_units/units/v7_bilingual_units.json`（或 `v7_units_as_cards.json`） | 4,973 个冻结双语知识单元，正式教材证据底座 | 新题检索和引用只读此类冻结 unit；新题本身不修改教材单元 |
| 基础依赖 | `phase3_index/output/index/v7_index_*.pkl`、`v7_unit_lookup.json`、`v7_embedding_index_meta.json` | 中文/英文 BM25、BGE 及 unit 查询索引 | 供盲判阶段调用；教材未更新时不需要因单题新增而重建 |
| 基础依赖 | `phase3_index/build_index.py` | 重建 v7 教材检索索引 | **仅**在 unit、术语或索引规则改变后运行，不是新增题的日常步骤 |
| 基础依赖（可选） | `v7/知识图谱提取/phases/phase06_kg_views/outputs/kg_retrieval_graph.json` | 知识图谱关系视图 | 开启 KG 扩展时用于补充候选上下文；其关系不是单独的正式题目证据 |
| 3 | `phase4_evidence/盲判流程/blind_adjudication.py` | DS 盲判主入口：多路检索、选项级判断、候选证据卡和机械校验 | 对新题按 `--question-id <新题ID>` 小批量运行；不向 DS 提供参考答案作为判断依据 |
| 3 | `盲判流程/s1_indexing.py` | 构造检索头、加载索引和相邻上下文 | 被主入口调用，不单独运行 |
| 3 | `盲判流程/s2_retrieval.py` | 编排 BGE/BM25、选项独立召回、KG 扩展和候选合并 | 被主入口调用，是 DS 初检索的核心 |
| 3 | `盲判流程/s3_candidate.py` | 统一候选记录格式 | 被主入口调用，不单独运行 |
| 3 | `盲判流程/s4_llm.py` | 构造盲判 prompt、调用/解析 DS 输出、过滤候选池外幻觉引用 | 被主入口调用；输出仍是机器草稿 |
| 3 | `盲判流程/s5_validation.py` | 对盲判结果作机械校验，如幻觉 unit、选项数量、答案格式和证据状态矛盾 | 被主入口调用；通过机械校验不等于内容已通过 Codex/人工审核 |
| 3 | `盲判流程/s6_output.py` | 写出逐题 JSON、汇总 JSONL 和盲判报告 | 被主入口调用，不单独运行 |
| 3 | `phase4_evidence/output/questions/q_v7_q_*.json` | 单题 DS 候选证据包：候选池、逐项判断、证据卡、预测答案与校验结果 | 新题的 DS 输出落在此处；是 Codex 二次核验的直接输入 |
| 4 | **Codex 二次核验任务（当前无独立生产脚本）** | 独立核验 DS 证据、重查教材、必要时查外部权威资料，标记题目状态 | 必须逐题执行；当前应以可留档的任务记录承载，不得把 DS JSON 直接视为最终结论 |
| 4 | `phase4_evidence/output/codex_review/<question_id>.json` | 存放 Codex 的纠错、补证、外部资料边界和最终状态 | 按第 4 节的最小 schema 建立并写入；当前无自动生产脚本，因此该文件是人工/Agent 留档关口 |
| 5 | `phase4_evidence/解析撰写/generate_evidence_explanations.py` | 基于盲判结果生成机器解析草稿；支持写回题目 JSON | 仅在 Codex 确认该题可进入解析后运行；若脚本尚未读取 Codex 核验状态，需人工控制运行范围 |
| 5 | `解析撰写/s1_explanation_data.py` 至 `s6_explanation_output.py` | 加载证据、组织材料、构造 prompt、规范化、复核和输出 | 被解析主入口调用，不单独运行 |
| 5 | `phase4_evidence/output/explanations/v7_q_*.md` | 单题解析母版 | 用于人工阅读和审核；不能替代题目 JSON 中的证据记录 |
| 6 | `phase4_evidence/质量审查/review_check.py` | 机械复核解析，生成需复核题清单；实际封装转调 `解析撰写/s5_explanation_review.py` | 新题生成解析后运行，检查引用、答案和格式问题 |
| 6 | `质量审查/quality_review.py` | 按语境偷换、主语借代、后合理化、页码幻觉、语气放大等模式做 LLM 深度复核 | 建议对新题、争议题和高风险题运行；它是质检，不替代人工批准 |
| 7 | `tools/v7_review_build/build_review_source.py` | 读取教材发布包和机器题目 JSON，生成供人工审核的审核源及指针 | 当前脚本扫描固定机器目录和固定日期下限，生成的是机器审核源，不读取 Codex/人工批准状态；运行前必须确认其扫描范围符合本批次 |
| 7 | `frontend/data/releases/v7/review-source/<release_id>/manifest.json` | 审核源批次清单、机器哈希、可审/拦截状态 | 人工审核按此清单处理；`reviewable` 不等于 `approved` |
| 8 | `tools/v7_release/build_release.py` | 将题目证据构建为前端题目发布包，并校验 unit ID 和状态 | 当前只识别机器 `pipeline_status=ok`；必须把已批准题复制到独立暂存目录后，才允许作为 `--evidence-dir` 输入 |
| 8（可选交付） | `解析撰写/export_software_explanations.py` | 将题目解析重组为软件导入所需的小节 Markdown | 需要交付第三方软件时运行 |
| 8（可选交付） | `解析撰写/apply_superscript.py` | 将小节 Markdown 的书内页码格式化为上标 | 在软件导出后、DOCX 导出前按需运行 |
| 8（可选交付） | `phase4_evidence/md-to-docx/md_to_docx.py`、`md_to_docx_en.py` | 将解析小节 Markdown 导出为中文/英文 DOCX | 只在需要 DOCX 交付时运行 |
| 非日常步骤 | `tools/v7_release/build_textbook_release.py` | 构建双语教材阅读发布包 | **不因新增题运行**；仅教材 PDF、冻结 unit 或页码映射更新时运行 |

### 9.3 新题执行时的最小命令序列

以下命令表示现有脚本的调用关系。执行前必须在新修订或新运行目录中工作，避免覆盖已审核/已发布资产。

**先处理当前脚本的覆盖风险：** `prepare_questions.py` 没有 `--output-dir` 参数，会直接重写 `phase3.5_questions/output/` 下的全量题库、章节映射、质量报告和 rejected 清单。并且它当前只生成基础字段；若正式输出中已有独立英文复核或人工补充字段，直接重跑可能造成字段丢失。故正式操作前必须完成：

1. 在版本控制分支或独立修订工作区中处理新题。
2. 备份 `phase3.5_questions/output/` 的四个输出文件，并记录哈希。
3. 在隔离副本中运行脚本，比较新旧输出的全量差异；只允许“新增题 + 经确认的必要字段变更”。
4. 确认英文补丁和人工字段未丢失后，才合并回主输出。若无法保证，先改造 `prepare_questions.py` 支持独立输出目录或稳定合并，**不要直接运行生产目录版本**。

```powershell
# 1. 新题已写入 CAMS_v7_questions.jsonl 后：重建标准化题库和质量报告
Set-Location "D:\守正公司工作区\cams考试\cams工作台（重构版）\v7\选项证据与解析生成\phase3.5_questions"
python prepare_questions.py

# 2. 对指定新题运行 DS 初检索与盲判
Set-Location "D:\守正公司工作区\cams考试\cams工作台（重构版）\v7\选项证据与解析生成\phase4_evidence\盲判流程"
python blind_adjudication.py --question-id v7_q_XXXXXX --concurrency 1

# 3. 此处停止自动流程：执行 Codex 独立核验与补证，并记录结论

# 4. 仅对 Codex 标记为可审核的题生成解析草稿
Set-Location "D:\守正公司工作区\cams考试\cams工作台（重构版）\v7\选项证据与解析生成\phase4_evidence\解析撰写"
python generate_evidence_explanations.py --output-dir ../output --question-id v7_q_XXXXXX --write-back

# 5. 运行机械解析复核；高风险题再运行 quality_review.py
Set-Location "D:\守正公司工作区\cams考试\cams工作台（重构版）\v7\选项证据与解析生成\phase4_evidence\质量审查"
python review_check.py --output-dir ../output --question-ids v7_q_XXXXXX
```

人工审核后，审核源和发布分别按以下规则处理：

```powershell
# 6. 构建机器审核源。注意：该脚本当前扫描固定 output/questions/，并非人工批准发布门禁。
Set-Location "D:\守正公司工作区\cams考试\cams工作台（重构版）\tools\v7_review_build"
python build_review_source.py

# 7. 人工批准后，以上一已验收发布批次的全部批准 q_*.json 为基线，
#    叠加本批批准题目，写入 release_input/<release-id>/questions/；
#    同目录保留 approved_questions.json。然后从发布器目录构建不可变题目证据包。
Set-Location "D:\守正公司工作区\cams考试\cams工作台（重构版）\tools\v7_release"
python build_release.py `
  --units "../../v7/work/base_units/units/v7_bilingual_units.json" `
  --freeze-manifest "../../v7/work/base_units/units/unit_freeze_manifest.json" `
  --questions "../../v7/选项证据与解析生成/phase3.5_questions/output/v7_questions.json" `
  --evidence-dir "../../v7/选项证据与解析生成/phase4_evidence/release_input/v7-YYYYMMDD/questions" `
  --output-dir "../../frontend/data/releases/v7/v7-YYYYMMDD" `
  --release-id "v7-YYYYMMDD"
```

`release_id` 采用 `v7-YYYYMMDD`；同日多批次追加 `-r1`、`-r2`。只有在发布包通过人工验收后才加 `--activate` 更新 `active.json`。验收人逐题核对：发布 manifest 的题目数、`approved_questions.json` 的题目数、前端显示的题目数、题干/选项/答案/解析/页码是否一致。

第 3 步是当前 SOP 的质量核心，也是自动脚本尚未封装的部分。后续若要批量稳定处理新题，应优先为 Codex 核验记录和人工批准记录定义可校验 schema，并让解析生成、审核源构建和发布器只接收 `codex_status=reviewable`、`human_status=approved` 的题；在此之前，严禁省略隔离运行、差异核对和发布暂存目录。
