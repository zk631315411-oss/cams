# Review 预留目录

本目录当前只有空的 `__init__.py`，没有实际审核或发布实现。

审核校验、人工决定和发布构建目前都在 `backend/storage.py`。该目录保留为未来可能的纯校验与发布组装边界，本轮不迁移代码。

若接手开发者后续提取逻辑，应保持：

- `storage.py` 仍是唯一文件写入层。
- `review` 只返回校验或组装结果，不直接写 `data` 或 `releases`。
- 提取前补足 V2 状态、版本绑定和发布回归测试。
