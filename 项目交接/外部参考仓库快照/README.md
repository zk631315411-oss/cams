# 外部参考仓库快照

本目录保存 CAMS 仓库内嵌套 Git 仓库的只读交接快照。压缩包包含各仓库的工作树、嵌套 `.git` 元数据、当前未提交修改和未跟踪文件；解压后可按原目录状态继续核对。

这些快照只用于完整交接和来源追溯，不是 CAMS 正式运行依赖。上游来源、固定提交和本地差异见本目录的 `MANIFEST.md`。

压缩包使用 Git LFS 管理。克隆交接仓库后需执行 `git lfs pull`，否则本目录只有 LFS 指针文件。
