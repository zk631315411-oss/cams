# Add this remote server to the Codex MCP configuration after the API content
# import. `codex` is a dedicated editor account; it cannot approve its own
# changes.
[mcp_servers.cams_workbench]
url = "http://127.0.0.1:8013/mcp"
