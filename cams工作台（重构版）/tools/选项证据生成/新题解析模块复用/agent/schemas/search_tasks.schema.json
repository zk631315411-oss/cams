{
  "type": "object",
  "required": ["search_tasks"],
  "properties": {
    "search_tasks": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["task_id", "target", "source_hypothesis", "search_mode", "queries", "must_terms", "why"],
        "properties": {
          "task_id": { "type": "string" },
          "target": { "type": "string" },
          "source_hypothesis": { "type": "string" },
          "search_mode": {
            "type": "string",
            "enum": ["semantic", "keyword", "exact", "contrast"]
          },
          "queries": {
            "type": "array",
            "items": { "type": "string" }
          },
          "must_terms": {
            "type": "array",
            "items": { "type": "string" }
          },
          "why": { "type": "string" }
        }
      }
    }
  }
}
