# Legacy scripts archive

These scripts were used during earlier extraction, alignment, filling, and review passes.

The active v8 workflow script remains one level up:
- `build_v8_text_refined.py`

The archived scripts are kept for traceability, but they are no longer part of the current v8 output workflow.
