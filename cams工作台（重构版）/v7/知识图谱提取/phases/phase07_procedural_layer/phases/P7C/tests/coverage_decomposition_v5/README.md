# P7C Coverage Decomposition v5

This patch-only iteration keeps the v2 Audit ledger and v4 strict evidence contract fixed.

It adds three narrowly scoped Patch corrections: distinguish initial from further investigation, create a separate minimal card when supplementing would break graph shape, and preserve required/need qualifiers in both process labels and edges.
