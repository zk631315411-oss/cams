# 章节修复修改清单

> 生成日期：2026-07-21
> 审计范围：CAMS 考试项目 v7 全链路（KG、章节映射、盲判、解析生成、软件导出、DOCX、模拟学生、检索验证、历史代码、输出产物）
> 修复目标：KG 知识图谱将教材 H1/H2/H3 层级全部展平产出 59 个 chapter_id（CH01-CH59），需还原为真实 16 章 + Glossary

---

## 总览

| 优先级 | 文件数 | 说明 |
|--------|--------|------|
| P0 必须修 | 6 个源文件 + 4 组数据文件 + 1 个 KG JSON | 不修则章节映射、盲判按章筛选、解析生成全部基于错误章节号 |
| P1 建议修 | 4 个源文件 + 输出产物 | 影响导出文件名、DOCX 标题、检索验证抽样；不影响核心逻辑但展示错误 |
| P2 可暂缓 | 2 个源文件（_attic）+ 模拟学生 | 历史实验代码和仅间接使用章节信息的脚本 |

---

## Part 分隔标题页处理说明

真实教材有 **4 个 Part 标题页**（仅标题 + 分隔装饰，非实质内容章）。OCR/KG 对它们的处理不一致：

| Part | 教材标题 | 印刷页 | KG 中状态 |
|------|----------|--------|-----------|
| Part 1 | Understanding the Risks and Methods of Financial Crime | 13 | **完全消失** — 无对应 KG 章节，第 13 页未被任何 chapter 的 printed_page_span 覆盖 |
| Part 2 | Global AFC Frameworks, Governance, and Regulations | 140 | **被提取为 CH18** — 标题一字不差，3 页、29 unit、3 section（引言+案例+要点），印刷页跨度 141-143 |
| Part 3 | Building an AFC Compliance Program | 240 | **未独立成章** — 标题页消失，但其开篇引言内容被吞入 CH33 "Introduction"（p241-266，7 section，76 unit） |
| Part 4 | Tools and Technologies to Fight Financial Crimes | 368 | **未独立成章** — 标题页消失，但其开篇引言内容被吞入 CH50 "Technology for AFC Compliance"（p369-371，3 页、19 unit、3 section） |

### CH18（Part 2 标题页）处理

CH18 标题完全匹配 Part 2 标题，内容为"引言+案例+要点总结"的 Part 开篇模块，**不属于任何真实教材章**。有 29 个 unit、3 个 section。

建议：
1. CH18 的 29 个 unit 归入 Ch5（Global AFC Standards and Guidance，紧随其后的真实章）
2. 如不删除 CH18，标记 `real_chapter: null`
3. 下游展示和导出中不将 CH18 视为独立考试章

### CH33（Part 3 引言 + Ch8 前半部分）处理

CH33 比 CH18 复杂——它有 7 个 section、76 个 unit、印刷页跨度 241-266。这 26 页的区间恰好覆盖：
- Part 3 标题页之后（p241-245 左右，引言 + Components of an AFC Program 概述）
- Ch8 正文前半部分（p246-264，Three lines of defense 等）

**需要拆分**：根据 section_title 判断每个 section 属于 Part 引言还是 Ch8 正文。Part 引言部分归入 Ch8（作为 Ch8 的引言），Ch8 正文部分保留在 Ch8。

### CH50（Part 4 引言）处理

CH50 结构同 CH18（3 页、3 section、引言+案例+要点），标题 "Technology for AFC Compliance" 接近 Part 4 标题但非逐字匹配。19 个 unit 建议归入 Ch13（紧随其后的真实章），作为 Ch13 的引言内容。

### Part 1 标题页

无内容丢失。Ch1 从第 14 页开始，Part 1 标题页（第 13 页）不存在于 KG 中，无需处理。

---

## P0: 必须修（核心链路断裂，不修无法工作）

### 1. KG 知识图谱本体

**文件**: `cams工作台（重构版）/v7/知识图谱提取/phases/phase06_kg_views/outputs/kg_retrieval_graph.json`

**影响范围**:
- `chapters` 列表：59 个条目，需新增 `real_chapter`（或 `parent_chapter`）字段，将每个 KG chapter 映射到真实 Ch1-Ch16/GLO/null
- `sections` 列表：339 个条目，每个有 `chapter_id`（如 CH01）和 `section_id`（如 CH01-S01），需新增 `real_chapter` 字段
- `units` 列表：4973 个条目，每个有 `chapter_id`（如 CH01），需新增 `real_chapter` 字段
- `core_points` 列表：983 个条目，每个有 `chapter_id`（如 CH01）和 `section_id`，需新增 `real_chapter` 字段
- `edges` 列表：8632 条边，其中：
  - `chapter_section` 边（339 条）：source_id 为 chapter_id（如 CH01），target_id 为 section_id — 需新增 `real_chapter` 字段标记 source 所属真实章
  - `cross_chapter_core_point` 边（1404 条）：连接属于不同 KG 章节的 core_point — 当两个 KG 章节合并到同一真实章时，这些边语义上变为"同真实章内的跨节边"，但 edge_scope 标签不再准确
  - `same_chapter_core_point` 边（229 条）：同理可能存在误标

**修改策略（推荐）**:
```
方案 A（最小侵入 — 新增字段，不改旧值）:
  - 为 chapters/sections/units/core_points 新增 real_chapter 字段
  - 旧 chapter_id 保留不变（CH01-CH59），所有下游引用不破坏
  - 新增 real_chapter 到 edges 的 source/target 元数据
  - 旧 edges 的 scope 字段暂时保留不修改

方案 B（彻底重构 — 重写 chapter_id）:
  - 将所有 chapter_id 从 CHxx 格式改为真实的 Chx 格式
  - sections 的 section_id 改为 Chx-Sxx 格式
  - core_point_id 改为 cp_Chx_Sxx_NNN 格式
  - 所有 downstream code 需同步修改
```

**建议采用方案 A**，理由：改动最小，下游代码可渐进式迁移。

**所需修改的具体字段/条目数**:

| 对象 | 计数 | 需新增字段 | 需修改/重算 |
|------|------|-----------|------------|
| chapters | 59 | `real_chapter: "Ch1"-"Ch16"` 或 `null`(CH18) 或 `"GLO"` | — |
| sections | 339 | `real_chapter`（从所属 chapter 继承） | — |
| units | 4973 | `real_chapter`（从所属 chapter 继承） | — |
| core_points | 983 | `real_chapter`（从所属 chapter 继承） | — |
| edges (chapter_section) | 339 | source/target 元数据新增 `real_chapter` | — |
| edges (cross_chapter_core_point) | 1404 | — | scope 字段日后可能需要重新计算（暂缓） |
| edges (same_chapter_core_point) | 229 | — | 同上 |

**是否破坏 core_point -> unit 关系**: 否。core_point 引用的 unit_ids（anchor_unit_ids, key_unit_ids, support_unit_ids）和 unit 列表中 unit 的归属关系不变。只新增 real_chapter 标记。

**真实的 real_chapter 映射表（需写入 KG）**:
```
CH01 → Ch1    CH02 → Ch1    CH03 → Ch1    CH04 → Ch1    CH05 → Ch1
CH06 → Ch2    CH07 → Ch2    CH08 → Ch2    CH09 → Ch2
CH10 → Ch3    CH11 → Ch3    CH12 → Ch3    CH13 → Ch3
CH14 → Ch4    CH15 → Ch4    CH16 → Ch4    CH17 → Ch4
CH18 → null  (Part 2 标题页)
CH19 → Ch5    CH20 → Ch5    CH21 → Ch5
CH22 → Ch6    CH23 → Ch6    CH24 → Ch6    CH25 → Ch6    CH26 → Ch6
CH27 → Ch7    CH28 → Ch7    CH29 → Ch7    CH30 → Ch7    CH31 → Ch7    CH32 → Ch7
CH33 → Ch8    CH34 → Ch8    CH35 → Ch8
CH36 → Ch9    CH37 → Ch9    CH38 → Ch9    CH39 → Ch9
CH40 → Ch10   CH41 → Ch10   CH42 → Ch10   CH43 → Ch10   CH44 → Ch10   CH45 → Ch10
CH46 → Ch11   CH47 → Ch11
CH48 → Ch12   CH49 → Ch12
CH50 → Ch13   CH51 → Ch13   CH52 → Ch13
CH53 → Ch14   CH54 → Ch14
CH55 → Ch15   CH56 → Ch15   CH57 → Ch15
CH58 → Ch16   CH59 → Ch16
```

另外，Glossary（GLO）在 KG 中是否有对应条目需确认。若无，需考虑是否新增 GLO 的章节条目。

---

### 2. 章节映射模块

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/题库章节映射/chapter_mapping.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 54 | `load_kg_catalog()` | `if len(chapters) != 59: raise RuntimeError(...)` | 改为 `if len(chapters) != 59: print(f"警告: KG 章节数={len(chapters)}，预期 59")` 或改为校验 real_chapter 存在性 |
| 48-55 | `load_kg_catalog()` | 构建 `{chapter_id: row}` 索引 | 需要新增 `real_chapter` 索引（`{chapter_id: real_chapter}`），供下游使用 |
| 161 | `aggregate_candidates()` | `kg_unit.get("chapter_id")` | 暂不改，但 candidate 中 `chapter_id` 仍为 CHxx，下游展示和映射文件会受影响 |
| 196 | `aggregate_candidates()` | `"chapter_id": chapter_id,` in candidate output | 需新增 `"real_chapter": real_chapter` 字段 |
| 308-309 | `validate_mappings()` | `if chapter_id not in catalog["chapters"]` | 校验逻辑暂不改，但 CH18 等特殊情况需加入白名单 |
| 312-322 | `validate_mappings()` | `catalog["sections"]` / `catalog["units"]` 归属校验 | 同上 |
| 335-358 | `build_batches()` | 按 `catalog["chapters"]` 的键生成 59 个 batch 文件 | **核心修改**：应改为按 `real_chapter` 生成 16+1 个 batch 文件（或继续按 CHxx 生成但文件名使用 real_chapter）。需决策：batch 文件命名用 CHxx（59 个）还是 Chx（16 个）？建议新增 real_chapter 文件 + 保留旧 CHxx 兼容 |
| 351-353 | `build_batches()` | `payload = {"chapter_id": cid, ...}` | 新增 `"real_chapter"` 字段 |
| 396-398 | `materialize_reviewed_mappings()` | mapping 的 `chapter_id` 写入 | 在 mapping 中新增 `real_chapter` 字段 |
| 460 | `decisions_from_review_policy()` | `chapter_ids` 来自 overrides | 决定文件中的 `chapter_ids` 目前用 CHxx，修复后仍需兼容旧决定文件 |
| 40-43 | 路径常量 | `DEFAULT_OUTPUT_DIR`, `DEFAULT_CANDIDATES_PATH`, `DEFAULT_MAPPINGS_PATH` | 路径常量本身不改，但输出内容中的 chapter_id 会变 |

**总结**：该文件核心修改点是 `build_batches()`（按 real_chapter 分组）和所有写入 chapter_id 的地方都需新增 real_chapter 字段。`len(chapters) != 59` 校验需放宽。

---

### 3. 章节映射数据文件（4 组）

**目录**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/题库章节映射/数据/`

#### 3a. chapter_batches/CHxx.json（59 个文件）

| 路径 | 当前内容 | 修改说明 |
|------|----------|----------|
| `数据/chapter_batches/CH01.json` ~ `CH59.json` | 每个文件含 `chapter_id: "CH01"`, `chapter_title`, `question_ids[]` | **必须重新生成**。新的 batch 文件建议按 real_chapter（Ch1-Ch16）组织，文件名改为 Ch01.json-Ch16.json。在每个 payload 中同时保留 `chapter_id: "CH01"`（KG 内部 ID）和 `real_chapter: "Ch1"`（真实章号）。或者：保留 59 个 CHxx 文件不动（作为细粒度批次），新增 16 个 Chx 文件（作为粗粒度汇总） |

#### 3b. question_chapter_mappings.jsonl

- 每条记录含 `question_id`, `chapter_mappings[]`, `mapping_status`
- `chapter_mappings[]` 中每项有 `chapter_id`（当前为 CH34, CH52 等 KG 内部 ID）
- **修改**：每条 mapping 新增 `real_chapter` 字段；`chapter_id` 保留不动
- 需全量重跑 `candidates` + `finalize` 子命令

#### 3c. question_chapter_mappings_agent.jsonl

- 与 3b 同结构，为 Agent 审核版本
- **修改**：同 3b

#### 3d. reviewed_decisions.json

- 包含 `overrides` 字典，key 为 question_id，value 中有 `chapter_ids` 数组（当前为 CH52, CH58 等）
- **修改**：重跑 finalize 后自动更新；如手动维护，需将 chapter_ids 沿用 KG 内部 CHxx 不变（因为 overrides 是给 decisions_from_review_policy 消费的）

#### 3e. chapter_similarity_candidates.jsonl（41 MB）

- 每条记录含 `chapter_candidates[]`，每项含 `chapter_id: "CH34"`, `chapter_title`
- **修改**：重跑 candidates 子命令后自动更新；新增 `real_chapter` 字段
- 否则下游 `materialize_reviewed_mappings()` 使用候选时缺少 real_chapter 信息

---

### 4. 盲判流程

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/盲判流程/blind_adjudication.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 316-397 | `load_kg_graph()` | 构建导航索引（cp_meta, unit_meta, cp_to_units 等） | 在 `unit_meta` 中保留 `real_chapter`；打印日志中新增 `real_chapters=` 统计 |
| 352-356 | `load_kg_graph()` | relation_scopes 集合含 `same_chapter_core_point`, `cross_chapter_core_point` | 这些 scope 标签是按 KG 章节的语义（例如 CH08 和 CH09 之间的 edge 标为 cross_chapter），当 CH08 和 CH09 合并到同一真实章 Ch2 后，这些 edge 语义变得不精确。暂不修改 scope 标签本身（因为会影响 expand_with_kg 中的路由权重），但建议在下游使用前根据 real_chapter 判断两个 core_point 是否真的跨章节 |
| 869-877 | `_relation_route()` | 映射 `same_chapter_core_point` → `kg_same_chapter_cp`，`cross_chapter_core_point` → `kg_cross_chapter_cp` | **关键**：当两个 KG 章节实际归属同一真实章时，`cross_chapter_core_point` 应该被视为 `same_chapter` 级别。建议在 `expand_with_kg()` 中新增检查：如果 `kg_index` 中的 `real_chapter_of[source_cp] == real_chapter_of[target_cp]`，则将 cross_chapter edge 降级为 same_chapter 路由（route_weight 0.72 而非 0.62） |
| 956-1111 | `expand_with_kg()` | 路由权重: kg_same_chapter_cp=0.72, kg_cross_chapter_cp=0.62 | 同上，同一真实章内的 cross_chapter edges 应提升权重 |
| 1953-1954 | `process_question()` | `result["chapter_mappings"] = question.get("_chapter_mappings")` | 映射中的 `chapter_id` 为 CHxx 格式，Result 中新增 `real_chapter` 字段或在 chapter_mappings 中嵌入 real_chapter |
| 2248-2252 | `main()` CLI | `--chapter-id` 参数（如 `--chapter-id CH01`） | **需同步修改**：用户输入 `--chapter-id Ch1`（真实章号）时，需通过 real_chapter 反向查找所有 CHxx 子章节，筛选题目 |
| 2365-2377 | `main()` chapter_id 筛选 | `mapping.get("chapter_id") == chapter_id` | 改为支持真实章号过滤（先查找 real_chapter 对应的所有 KG chapter_id，再匹配） |
| 2121 | `write_summary_jsonl()` | `"chapter_mappings": r.get("chapter_mappings", [])` | 汇总 JSONL 中的 chapter_mappings 需包含 real_chapter 信息 |

**section_context 扩展逻辑**：当前 `heading_context` 从 unit_lookup 直接读取（line 1164-1165），它来自 phase3 索引，与 KG 的 chapter_id 无关，故不受影响。

---

### 5. 解析生成

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/解析撰写/generate_evidence_explanations.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 440-443 | `build_prompt()` | `chapter_text_value = "；".join(f"{row.get('chapter_id', '')} {row.get('chapter_title', '')}")` | 这是写入 LLM prompt 的章节展示文本。若 KG 修复后 chapter_title 仍是展平状态（如 "Types of financial crime" 而非 "Ch1 - Types of financial crime"），需要决定是用 real_chapter 的标题（如 "Ch1 Money Laundering and Financial Crime"）还是细粒度的 KG 章节标题。建议在 prompt 中同时展示 real_chapter 和 KG 章节 |
| 498-499 | `build_prompt()` prompt 文本 | `教材章节：{chapter_text_value}` | 同上 |
| 1380-1387 | `chapter_text()` | 从 result["chapter_mappings"] 读取 chapter_id 和 chapter_title 拼接 | 同 prompt 逻辑，建议新增 real_chapter 展示 |
| 1590-1598 | `collect_generated_rows()` | `row["chapter_mappings"] = result.get("chapter_mappings", [])` | chapter_mappings 中的 chapter_id 为 CHxx，软件就绪判断不直接依赖此字段，但展示时需考虑 |
| 1602-1625 | `write_index()` | 表格中章节列取 `item.get("chapter_id", "")` | **修改**：改为取 `real_chapter` 展示，或同时展示 real_chapter + KG chapter_id |
| 1628-1667 | `write_chapter_drafts()` | 按 `chapter_id` 分组生成草稿文件，文件名 `{chapter_id}.md`（如 CH34.md） | **核心修改**：分组逻辑应改为按 `real_chapter` 分组，文件名改为 Ch08.md；或按 real_chapter 分组但保留 KG 子章节细分 |
| 1371 | `normalize_explanation()` | `"chapter_mappings": result.get("chapter_mappings", [])` | 解析母版 JSON 中 chapter_mappings 需确保 real_chapter 可用 |

---

### 6. 软件导出

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/解析撰写/export_software_explanations.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 188-205 | `belongs_to_chapter()` | `m.get("chapter_id") == chapter_id` | **需支持真实章号匹配**：检查 `m.real_chapter` 或从外部映射反向查找 |
| 208-295 | `export_chapter()` | 按 `chapter_id` 筛选并导出，章节预览文件命名 `{chapter_id}.md` | 预览文件名应改为真实章号（如 Ch08.md 而非 CH34.md） |
| 253-260 | `export_chapter()` | Markdown 标题 `# {chapter_id} 题库软件版解析预览` | 标题展示真实章号 |
| 303 | `main()` CLI | `--chapter-id` 参数 | 用户传入的可能是真实章号（Ch1-Ch16）或 KG 章节号（CH01-CH59），需同时支持 |

---

## P1: 建议修改（影响展示和验证，但不阻断核心逻辑）

### 7. DOCX 生成

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/md-to-docx/md_to_docx.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 98 | `parse_question_block()` | `result['chapters'] = stripped.replace('教材章节：', '')` | 从 Markdown 解析出的章节文本，当前可能为 "CH34 Three lines of defense" 或已改为 real_chapter 格式。若上游 generate_evidence_explanations 已修复，此处自动正确 |
| 145-152 | `add_question_to_doc()` | 跨章节标记：按 `；` 分割提取 chapter_ids 前缀（`c.strip().split(' ')[0]`） | 跨章节检测逻辑依赖章节文本格式。若使用 real_chapter，则 Ch1；Ch2 等格式仍可正确解析。标记显示为 `【跨章节：CH01、CH06】` 需改为 `【跨章节：Ch1、Ch2】` |
| 236 | CLI | `-i` 输入文件命名 CHxx.md | 如上游 draw_chapter_drafts 改为 Chx.md 命名，则 DOCX 输入文件名也自然跟着变。无需额外修改 |

---

### 8. 检索验证

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/盲判流程/retrieval_validation.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 179, 192-197 | `sample_questions()` | 按 `chapter_code` 字段（来自旧题库 JSON）分组抽样；`default_target` 硬编码 `CH01=7, CH03=4, CH04=1, CH05=3` | 这是从旧题库 `v7_questions.json` 的 `chapter_code` 字段读取的，与 KG 的 chapter_id 来源不同。旧题库中的 `chapter_code` 如何编码真实章号需确认。如果旧题库的 chapter_code 正确对应真实章，此处无需修改。但如果旧题库也使用展平后的编号，则需要改。需检查 `v7_questions.json` 中 chapter_code 的实际值 |
| 496, 504 | `write_markdown()` | 报告展示 `ch = q.get("chapter_code", "?")` | 同上 |
| 626 | `main()` | 进度打印 `ch = q.get("chapter_code", "?")` | 同上 |

**注意**：此文件使用的是 `chapter_code`（旧题库字段），而非 KG 的 `chapter_id`。需先确认旧题库的 chapter_code 是否正确。

---

### 9. 已生成的输出产物

**目录**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/output/`

产出物清单（含 chapter_id 字段）：

| 路径/模式 | 说明 | 处理方式 |
|-----------|------|----------|
| `output/questions/q_*.json` | 每题盲判详情，含 `chapter_mappings[]`（chapter_id 为 CHxx） | 重跑盲判后自动修复；旧文件可标记为 _pre_fix 或删除 |
| `output/blind_judgment_results.jsonl` | 汇总文件，含 `chapter_mappings` | 重跑后重新生成 |
| `output/blind_judgment_report.md` | 人读报告 | 重跑后重新生成 |
| `output/explanations/*.md` | 每题 V3.1 解析 Markdown，含 `教材章节：CHxx...` | 重跑解析生成后修复 |
| `output/explanations/index.md` | 解析索引，章节列含 CHxx | 重跑后修复 |
| `output/explanations/chapters/CHxx.md` | 按 KG 章节分组的草稿 | 重跑后文件名和内容均需更新为 Chx 格式 |
| `output/software_export/chapters/CHxx.md` | 软件导出预览 | 重跑 export 后修复 |
| `output/software_export/export_results.json` | 导出汇总 | 重跑后修复 |

**目录**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/output/` 下按章节分的子目录：

| 路径 | 说明 |
|------|------|
| `output/CH01/` ~ `output/CH09/` | 按 KG 章节分的盲判输出目录 |
| `output/rerun_ch01/` | 重跑目录 |
| `output/demo_ch01/` ~ `output/demo_ch03/` | Demo 运行目录 |

这些目录名使用 CHxx 格式，需重命名为 Chx 或添加符号链接。

---

## P2: 可暂缓（不影响核心功能）

### 10. 历史代码 (_attic/)

**目录**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/_attic/`

受影响的 _attic Python 文件：

| 文件 | 受影响原因 | 紧急程度 |
|------|-----------|----------|
| `stepwise_retrieval/s2/s2_kg_expansion.py` | 引用 KG_GRAPH_PATH，硬编码 `EDGE_SCOPE_WEIGHTS` 含 `same_chapter_core_point` 和 `cross_chapter_core_point` | 低：实验代码，不用于生产 |
| `_archive/judgment_intent_b/judgment_intent_b/run_intent_adjudication.py` | 导入旧版 `blind_adjudication` 脚本（注意路径指向 `scripts/` 而非 `盲判流程/`） | 极低：归档代码，branch B 实验 |
| `kg_evidence_packet_experiment.py` | KG 证据包实验，可能引用 chapter_id | 低：实验代码 |
| `multi_head_retrieval_experiment.py` | 多头检索实验 | 低：实验代码 |
| `stepwise_retrieval/s1/s1_direct_unit_retrieval.py` | 直接检索实验 | 低 |
| `stepwise_retrieval/s0/s0a_p5_heads.py` 等 | P5 术语实验 | 低 |
| `stage_explanation_run.py` | 分段解析运行 | 低 |

**建议**：在 _attic 目录下新增 `README.md` 说明这些文件基于 59 章 KG，实际 16 章结构不同。代码本身不修改。

#### 已生成的 _attic 输出产物

| 路径 | 说明 |
|------|------|
| `_attic/blind_judgment_results.jsonl` | 旧盲判结果 |
| `_attic/blind_judgment_report.md` | 旧盲判报告 |
| `_attic/output/kg_packets/` | KG 证据包输出 |
| `_attic/output/multi_head_retrieval/` | 多头检索输出 |
| `_attic/q140_*/` `_attic/q286_*/` | 单题专项测试输出 |

这些文件为历史快照，仅作参考，不需要修改。

---

### 11. 模拟学生

**文件**: `cams工作台（重构版）/v7/选项证据与解析生成/phase4_evidence/模拟学生/simulate_student.py`

| 行号 | 位置 | 当前内容 | 修改说明 |
|------|------|----------|----------|
| 56-66 | `build_student_prompt()` | `heading_context` 从 `source_evidence` 读取，包含章节路径（如 "CH34 > Three lines of defense"） | heading_context 来自 unit_lookup，与 KG chapter_id 无关。但如果 KG 的 heading_context 字段使用 CHxx 前缀，则改为真实章号前缀后此处展示自动正确 |
| 38-65 | `build_student_prompt()` | 整个 prompt 无直接 chapter_id 引用 | 无直接修改必要 |

**结论**：模拟学生脚本不直接使用 chapter_id，只间接展示 heading_context。若 KG 修复后 heading_context 更新，此处自动受益。

---

## 修改执行顺序建议

1. **首先**修复 KG 知识图谱 JSON（P0-1），新增 `real_chapter` 字段
2. 基于新的 KG，**重跑** chapter_mapping.py `candidates` 子命令，生成新候选文件（P0-3e）
3. **更新** reviewed_decisions.json 中的 chapter_ids（如有手动维护），**重跑** `finalize` 子命令（P0-3d）
4. **重跑** `validate` 子命令，生成新的 chapter_batches 和 mapping_review（P0-3a, 3b, 3c）
5. **修改** chapter_mapping.py 源码中的 `len(chapters) != 59` 校验和 build_batches 逻辑（P0-2）
6. **修改** blind_adjudication.py 中的 `--chapter-id` 筛选和 expand_with_kg 路由权重（P0-4）
7. **修改** generate_evidence_explanations.py 中的章节展示和分组逻辑（P0-5）
8. **修改** export_software_explanations.py 中的 belongs_to_chapter 和 export_chapter（P0-6）
9. **重跑**盲判流程，更新 output/questions/ 和 output/explanations/（P1-9）
10. **修改** md_to_docx.py 和 retrieval_validation.py（P1-7, P1-8）
11. 在 _attic 目录加说明文件（P2-10）

---

## 附录：完整文件清单

### P0 文件（6 源文件 + 1 KG JSON + 4 组数据文件）

| # | 文件路径（相对于 `cams工作台（重构版）/v7/`） | 类型 |
|---|------|------|
| 1 | `知识图谱提取/phases/phase06_kg_views/outputs/kg_retrieval_graph.json` | KG JSON |
| 2 | `选项证据与解析生成/phase4_evidence/题库章节映射/chapter_mapping.py` | Python |
| 3 | `选项证据与解析生成/phase4_evidence/题库章节映射/数据/chapter_batches/CH01.json` ~ `CH59.json` | 数据 (59 文件) |
| 4 | `选项证据与解析生成/phase4_evidence/题库章节映射/数据/question_chapter_mappings.jsonl` | 数据 |
| 5 | `选项证据与解析生成/phase4_evidence/题库章节映射/数据/question_chapter_mappings_agent.jsonl` | 数据 |
| 6 | `选项证据与解析生成/phase4_evidence/题库章节映射/数据/reviewed_decisions.json` | 数据 |
| 7 | `选项证据与解析生成/phase4_evidence/题库章节映射/数据/chapter_similarity_candidates.jsonl` | 数据 |
| 8 | `选项证据与解析生成/phase4_evidence/盲判流程/blind_adjudication.py` | Python |
| 9 | `选项证据与解析生成/phase4_evidence/解析撰写/generate_evidence_explanations.py` | Python |
| 10 | `选项证据与解析生成/phase4_evidence/解析撰写/export_software_explanations.py` | Python |

### P1 文件（4 源文件 + 输出产物）

| # | 文件路径 | 类型 |
|---|------|------|
| 11 | `选项证据与解析生成/phase4_evidence/md-to-docx/md_to_docx.py` | Python |
| 12 | `选项证据与解析生成/phase4_evidence/盲判流程/retrieval_validation.py` | Python |
| 13 | `选项证据与解析生成/phase4_evidence/output/questions/q_*.json` | 输出 |
| 14 | `选项证据与解析生成/phase4_evidence/output/explanations/**/*.md` | 输出 |
| 15 | `选项证据与解析生成/phase4_evidence/output/software_export/**/*.md` | 输出 |

### P2 文件（2 源文件 + _attic 若干）

| # | 文件路径 | 类型 |
|---|------|------|
| 16 | `选项证据与解析生成/phase4_evidence/_attic/stepwise_retrieval/s2/s2_kg_expansion.py` | Python (_attic) |
| 17 | `选项证据与解析生成/phase4_evidence/_attic/_archive/judgment_intent_b/judgment_intent_b/run_intent_adjudication.py` | Python (_attic) |
| 18 | `选项证据与解析生成/phase4_evidence/模拟学生/simulate_student.py` | Python (无直接修改) |
